import styled from 'styled-components';

export const Container = styled.select`
  width: 560px;
  height: 80px;
  border: 0;
  outline: 0;

  background-color: #fff;
  border-radius: 5px;

  font-family: Poppins;
  font-weight: bold;
  font-size: 28px;
`;
